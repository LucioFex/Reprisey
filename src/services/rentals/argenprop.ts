// import cheerio from 'cheerio';
import { IArgenpropData } from '../../utils/interfaces/rentalsInterfaces';

const argenprop = (location: string, query: unknown): IArgenpropData[] => {
    // TODO
};

export default argenprop;
